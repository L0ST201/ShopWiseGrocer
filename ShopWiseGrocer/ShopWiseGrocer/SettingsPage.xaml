<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:models="clr-namespace:ShopWiseGrocer.Models"
             x:Class="ShopWiseGrocer.SettingsPage"
             Title="Settings"
             BackgroundColor="{DynamicResource AppBackground}">

    <StackLayout Padding="20">
        <Label Text="Theme Mode" FontSize="24" />

        <Slider Maximum="1"
            Minimum="0"
            Value="{Binding Source={x:Reference App}, Path=IsDarkTheme, Mode=TwoWay}"
            WidthRequest="200"
            Margin="0,10,0,10" />


        <Label Text="{Binding ThemeModeText}"
               FontSize="16"
               HorizontalOptions="Center" />

    </StackLayout>

</ContentPage>
