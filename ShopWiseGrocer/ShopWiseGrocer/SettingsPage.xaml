<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:models="clr-namespace:ShopWiseGrocer.Models"
             x:Class="ShopWiseGrocer.SettingsPage"
             Title="Settings"
             BackgroundColor="{DynamicResource AppBackground}">

    <Grid Padding="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackLayout Padding="20" VerticalOptions="CenterAndExpand">

            <Label Text="Language:" FontSize="20" />

            <Frame HasShadow="True" CornerRadius="5" Padding="0" BackgroundColor="White">
                <Picker x:Name="LanguagePicker" 
                        Title="Select a Language"
                        SelectedIndexChanged="OnPickerSelectedIndexChanged"
                        Margin="0,10,0,10" />
            </Frame>

            <Label Text="Font Size" FontSize="20" />
            
            <Frame HasShadow="True" CornerRadius="5" Padding="0" BackgroundColor="Gray">
                <Slider x:Name="FontSizeSlider"
                    Maximum="30"
                    Minimum="10"
                    ValueChanged="OnSliderValueChanged"
                    BackgroundColor="Gray"
                    Margin="0,10,0,10" />
            </Frame>

        </StackLayout>
    </Grid>
</ContentPage>
